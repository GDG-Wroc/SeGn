# Design by Analogy MCP Server

Streamable HTTP MCP server for a generic, multi-step Design by Analogy method.

Run:

```bash
uv sync
uv run python app/main.py
```

Default endpoint: `http://127.0.0.1:8124/mcp`
